import { Box, Heading, Text } from '@chakra-ui/react';

export default function TrainingDashboard() {
  return (
    <Box p={6}>
      <Heading size="md">Training Portal</Heading>
      <Text mt={2}>LMS UI, progress tracking, certifications (stubs).</Text>
    </Box>
  );
}


