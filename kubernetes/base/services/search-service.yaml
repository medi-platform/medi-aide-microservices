apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: medi-aide
  labels:
    app.kubernetes.io/name: search-service
    app.kubernetes.io/part-of: stage3
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: search-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: search-service
        app.kubernetes.io/part-of: stage3
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: search-service
          image: REPLACE_ME_REGISTRY/medi-aide-search-service:REPLACE_TAG
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4022
              name: http
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "4022"
            - name: SERVICE_NAME
              value: "search-service"
            - name: SERVICE_ROUTE_PREFIX
              value: "search"
            - name: SERVICE_ADDRESS
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: CONSUL_HOST
              value: "consul.medi-aide.svc.cluster.local"
            - name: CONSUL_PORT
              value: "8500"
            - name: ELASTICSEARCH_URL
              valueFrom:
                secretKeyRef:
                  name: search-service-es
                  key: url
                  optional: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /search/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /search/health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: medi-aide
spec:
  selector:
    app.kubernetes.io/name: search-service
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: search-service
  namespace: medi-aide
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: search-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: search-service
  namespace: medi-aide
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: search-service
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-namespace-and-egress-search-service
  namespace: medi-aide
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: search-service
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: medi-aide
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: medi-aide
      ports:
        - protocol: TCP
          port: 8500
        - protocol: TCP
          port: 9200


